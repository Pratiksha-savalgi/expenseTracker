# UI Design Prototypes

## How to open (important)

Cursor may show **Unable to open index.html** if you click a `d:\...` path link (bad URI like `d:%5C...`). The files are fine — open them this way instead:

1. Double-click `open-design-prototypes.bat` in the project root, **or**
2. Double-click `docs/design/prototypes/OPEN.bat`, **or**
3. In File Explorer go to `d:\expenseTracker\docs\design\prototypes\` and double-click `index.html`

Do **not** rely on Cursor chat path links to preview HTML.

| Option | Feel |
|--------|------|
| **B+C (combined)** | Soft panels + summary cards (B) with compact expense table + teal (C) — **current pick to review** |
| A / B / C | Original separate prototypes (still available) |

Static mockups only. Prefer opening via `open-design-prototypes.bat` (opens B+C login).

**Tell me:** approve B+C, or list edits.
